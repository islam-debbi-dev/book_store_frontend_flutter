import 'package:flutter/material.dart';

class AdminAuthorsPage extends StatelessWidget {
  const AdminAuthorsPage({super.key});
  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Authors Management Page'));
  }
}
